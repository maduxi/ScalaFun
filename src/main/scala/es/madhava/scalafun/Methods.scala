package es.madhava.scalafun

object Methods {

  def ternary(input:String):Boolean = {
    if (input == "test") true else false
  }

  def apply(a: Int): Int = {
    a * 2
  }

  def update(a: Int, value:String): Int = {
    a * 4
  }
}
